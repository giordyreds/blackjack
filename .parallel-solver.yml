# .parallel-solver.yml
forge: github
sandbox: docker
concurrency: 3

docker:
  dockerfile: ./sandbox.Dockerfile
  claudePlugins:
    - mattpocock-skills@mattpocock

git:
  identity:
    name: Blackjack Solver Bot
    email: blackjack-solver@users.noreply.github.com

baseBranch: master

dependencies:
  enabled: true

labelFilter: ready-for-agent
